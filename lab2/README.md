
# Отчет по лабораторной работе №2: «Интерактивность веб-приложений. JavaScript»

* **Дисциплина:** Построение современных веб-интерфейсов / Проектирование сетевых приложений
* **Студент:** Ковригина Д. М. (Группа ИУ5-42Б)

---

## Оглавление
1. [Цель и задачи лабораторной работы](#цель-и-задачи-лабораторной-работы)
2. [Индивидуальная тематика и стилистика](#индивидуальная-тематика-и-стилистика)
3. [Теоретическая база применяемых технологий](#теоретическая-база-применяемых-технологий)
4. [Архитектурные решения и фрагменты кода](#архитектурные-решения-и-фрагменты-кода)
   * [Логика переключения тем (JavaScript)](#логика-переключения-тем-javascript)
   * [Эффект матового стекла (Glassmorphism)](#эффект-матового-стекла-glassmorphism)
   * [Блокировка прокрутки интерфейса](#блокировка-прокрутки-интерфейса)
5. [Вывод](#вывод)

---

## Цель и задачи лабораторной работы

**Цель:** Овладение практическими навыками программирования интерактивных пользовательских интерфейсов с использованием языка JavaScript, а также закрепление продвинутых методов верстки (HTML5/CSS3).

**Задачи, реализованные в ходе работы:**

1. Оживление статического макета: добавление скриптов для взаимодействия с пользователем.
2. Программирование логики переключения визуальных тем (Тёмная/Светлая) с динамической заменой фоновых изображений и текстового контента кнопок.
3. Разработка многостраничной структуры проекта (`calculator.html`, `goal.html`, `author.html`) с единой навигационной панелью (`header`) и закрепленным подвалом (`footer`).
4. Применение современных визуальных эффектов: Glassmorphism (эффект матового стекла) и плавные анимации (`transition`).
5. Организация правильной архитектуры проекта и работа с системой контроля версий Git (Feature Branch Workflow).

---

## Индивидуальная тематика и стилистика

В рамках выполнения требования по уникальности интерфейса была выбрана следующая концепция:

* **Тематика:** Терминал управления лунной исследовательской станцией "Sanctuary".
* **Стилистический референс:** Футуристичные интерфейсы аэрокосмических симуляторов и научно-фантастических панелей управления.

Интерфейс построен на базе эффекта **Glassmorphism** — элементы управления представляют собой полупрозрачные «стеклянные» панели, сквозь которые виден космический фон. Реализованы две атмосферные темы:
* **Тёмная тема (базовая):** Строгий интерфейс на фоне классической поверхности Луны.
* **Светлая тема (альтернативная):** Адаптированный интерфейс на фоне лунохода (`Moon_rover`), демонстрирующий работу на поверхности.

---

## Теоретическая база применяемых технологий

При разработке интерактивной части были применены следующие концепции:

1. **Событийно-ориентированное программирование:** Обработка пользовательского ввода с помощью слушателей событий (событие `onclick` на кнопках смены темы).
2. **Glassmorphism в CSS:** Применение свойства `backdrop-filter: blur()` в сочетании с полупрозрачными фонами (`rgba`) для создания эффекта глубины и многослойности интерфейса.
3. **Управление переполнением контента:** Решение проблемы длинных чисел на экране калькулятора с помощью свойств `overflow-x: auto` и скрытия стандартного скроллбара.

---

## Архитектурные решения и фрагменты кода

### Логика переключения тем (JavaScript)

Для смены тем написана единая универсальная функция `toggleTheme`, которая управляет состоянием всего приложения.

```javascript
// Фрагмент файла script.js
const headerThemeBtn = document.getElementById('header_theme_btn');
const moonImg = document.querySelector('.moon-bg');

function toggleTheme() {
    // Переключаем глобальный класс на body
    document.body.classList.toggle('alternate-theme');

    // Проверяем текущую тему и адаптируем контент
    if (document.body.classList.contains('alternate-theme')) {
        moonImg.src = 'Moon_rover.png';
        if (headerThemeBtn) headerThemeBtn.textContent = 'ТЕМНАЯ ТЕМА';
    } else {
        moonImg.src = 'Moon.png';
        if (headerThemeBtn) headerThemeBtn.textContent = 'СВЕТЛАЯ ТЕМА';
    }
}

if (headerThemeBtn) {
    headerThemeBtn.onclick = toggleTheme;
}
```

### Эффект матового стекла (Glassmorphism)

Для визуального отделения элементов управления от космического фона используется размытие заднего плана.

```css
/* Фрагмент файла style.css */
.main-header, .main-footer, .calculator-panel {
    background: rgba(0, 0, 0, 0.7); /* Полупрозрачный темный фон */
    backdrop-filter: blur(10px);    /* Размытие фона под элементом */
    border: 1px solid rgba(255, 255, 255, 0.1); /* Тонкая светлая рамка */
}
```

### Блокировка прокрутки интерфейса
Для того чтобы приложение ощущалось как нативный терминал, а не как обычный веб-сайт, была жестко зафиксирована высота экрана и отключена системная прокрутка.

```css
body {
    margin: 0;
    padding: 0;
    height: 100vh; /* Строго 100% высоты экрана */
    overflow: hidden; /* Полный запрет появления скроллбаров */
    display: flex;
    justify-content: center;
    align-items: center;
}
```
---

### Вывод
В ходе выполнения лабораторной работы статический макет был успешно преобразован в интерактивное многостраничное веб-приложение. Настроена удобная навигация между страницами, реализована стабильно работающая функция смены визуальных тем с помощью JavaScript, а также исправлены мелкие баги UI (например, переполнение экрана калькулятора). Проект имеет чистую архитектуру, разложен по папкам и корректно загружен в репозиторий GitHub с использованием веток (branches). База для дальнейшего программирования математической логики калькулятора полностью готова.
